/*

const express = require("express");
const router = express.Router();
const entidadController = require("../controllers/entidad.controller");

router.post("/", entidadController.create)
router.get("/", entidadController.find)
router.get("/:id", entidadController.findOne)
router.put("/:id", entidadController.update)
router.delete("/:id", entidadController.remove)

module.exports = router;

*/